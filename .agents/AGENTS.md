# Workspace Agent Rules

## Automated Git Push Policy
- After completing any new feature, bug fix, visual modification, or code update in this workspace, always stage (`git add .`), commit with a descriptive message (`git commit -m "..."`), and push (`git push origin main`) to GitHub automatically before finishing the task.
